## 1. Core Implementation

- [x] 1.1 Import `datetime` in `orchestrate.py`
- [x] 1.2 Update `_pipe_to_log` to prepend formatted timestamps to forwarded child output lines

## 2. Verification

- [x] 2.1 Run the orchestrator and verify timestamp prefixes on cloud and scraper lines
