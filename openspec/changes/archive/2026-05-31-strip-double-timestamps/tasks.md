## 1. Core Implementation

- [x] 1.1 Import `re` in `orchestrate.py`
- [x] 1.2 Define `TIMESTAMP_RE` regex pattern in `orchestrate.py`
- [x] 1.3 Update `_pipe_to_log` to strip duplicate timestamp prefixes using the regex

## 2. Verification

- [x] 2.1 Run the orchestrator and verify that scraper and cloud lines do not have duplicate timestamps
