# env / venv / caches in any subproject
**/.env
**/.venv/
**/__pycache__/
*.pyc
*.log
*.err
.DS_Store

# local scraper runtime state
local/playwright_profile/
local/scraper_state.json

# uv keeps its lock files checked in, so don't ignore them
